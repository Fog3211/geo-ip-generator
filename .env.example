# Database
# SQLite database file path
DATABASE_URL="file:./prisma/db.sqlite"

# Redis Cache (Optional)
# Uncomment and configure if you want to enable caching
# REDIS_URL="redis://localhost:6379"

# Node Environment
NODE_ENV="development"

# Dataset source for prestart (imported into DB before service starts)
# Supports http/https URL, absolute/relative file path, or file:// URL
# Default recommended: GitHub Releases latest asset (faster than raw and repo checkout)
GEO_DATA_URL="https://github.com/<owner>/<repo>/releases/latest/download/combined-geo-ip-data.json"

# Optional backup dataset URL when primary fails
GEO_DATA_BACKUP_URL="https://raw.githubusercontent.com/<owner>/<repo>/main/data/combined-geo-ip-data.json"

# Optional: Skip environment validation during build
# SKIP_ENV_VALIDATION="false"
